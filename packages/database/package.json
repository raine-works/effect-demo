{
	"name": "@effect-demo/database",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"types": "./.types/index.d.ts",
	"main": "./src/index.ts",
	"scripts": {
		"migrate": "bunx --bun prisma migrate dev --schema=./prisma/schema.prisma",
		"build": "tsc && bunx --bun prisma generate",
		"reset": "DATABASE_URL='postgresql://effect_demo_dev_user:effect_demo_dev_password@localhost:5432/effect_demo_dev_db?schema=public' bunx --bun prisma db push --force-reset"
	},
	"dependencies": {
		"@effect-demo/tools": "workspace:*",
		"@prisma/client": "catalog:"
	},
	"devDependencies": {
		"@effect-demo/tsconfig": "workspace:*",
		"@prisma/adapter-pg": "catalog:",
		"@types/node": "catalog:",
		"bun-types": "catalog:",
		"prisma": "catalog:"
	},
	"peerDependencies": {
		"typescript": "catalog:"
	}
}
