{
	"name": "@effect-demo/database",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"types": "./.types/index.d.ts",
	"main": "./src/index.ts",
	"scripts": {
		"build": "bunx --bun prisma generate && tsc",
		"format": "bunx --bun prisma format",
		"migrate": "bunx --bun prisma migrate dev --schema=./prisma/schema.prisma",
		"reset": "DATABASE_URL='postgresql://effect_demo_dev_user:effect_demo_dev_password@localhost:5432/effect_demo_dev_db?schema=public' bunx --bun prisma migrate reset",
		"studio": "DATABASE_URL='postgresql://effect_demo_dev_user:effect_demo_dev_password@localhost:5432/effect_demo_dev_db?schema=public' bunx --bun prisma studio"
	},
	"dependencies": {
		"@effect-demo/tools": "workspace:*",
		"@prisma/client": "catalog:"
	},
	"devDependencies": {
		"@effect-demo/tsconfig": "workspace:*",
		"@prisma/adapter-pg": "catalog:",
		"@types/node": "catalog:",
		"bun-types": "catalog:",
		"prisma": "catalog:"
	},
	"peerDependencies": {
		"typescript": "catalog:"
	}
}
